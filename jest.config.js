// Pure-logic unit tests run under the lightweight Node preset from
// expo-module-scripts. Hook tests that need React Native can adopt the full
// `expo-module-scripts/jest-preset` (ios/android/web/node) later.
module.exports = require("expo-module-scripts/jest-preset-utils");
